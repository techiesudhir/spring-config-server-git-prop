server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/techiesudhir/spring-config-server-git-prop
          #username: 
          #password: 
          search-paths:
          - app*
          force-pull: true